# Legacy / Reference System

## Status
N/A — greenfield. This project does not port or mirror any existing system, so
there are no parity concerns. Agents should ignore legacy/parity considerations.

## Reference Source Location
- **Path / repo**: N/A — greenfield

## Source-of-Truth Priority
N/A — greenfield. The approved spec (`PROJECT_SPEC.md` and any `specs/`) is the
single source of truth.

## Parity Rules
N/A — greenfield. Nothing must match a legacy system; the design is free.
