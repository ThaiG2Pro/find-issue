# Test Cases — digest-renderer-5
Generated by: qa | Date: 2026-06-29 | Phase: S5

Export format: md (testcase_export = "md")

## Coverage Summary

| Total ACs | ACs Covered | Scenarios | Priority Distribution |
|-----------|-------------|-----------|----------------------|
| 20 | 20 (100%) | 32 scenarios | 15 HIGH / 12 MEDIUM / 5 LOW |

---

## Test Cases

| TC-ID | AC-ID | Scenario | Input | Expected | Priority | Verification Method | Status |
|-------|-------|----------|-------|----------|----------|---------------------|--------|
| TC-001 | AC-5-001 | Non-empty list → non-empty str with `## repo` and `- #id` lines | `[SummarizedItem(repo="owner/repo", item_id="42")]`, `lookback_days=7` | `isinstance(result, str)`, `"## owner/repo" in result`, `"- #42" in result` | HIGH | Run `render([item], lookback_days=7)` and assert type + structure | PASS |
| TC-002 | AC-5-001 | Adapter class output matches free function | Same items list rendered via both `render()` and `MarkdownDigestRenderer().render()` | Both return byte-equal strings | HIGH | Assert `MarkdownDigestRenderer().render(items, lookback_days=7) == render(items, lookback_days=7)` | PASS |
| TC-003 | AC-5-001 | Output always starts with `# OSS Pulse Digest` title | Any non-empty item list | `result.startswith("# OSS Pulse Digest")` | HIGH | String prefix check | PASS |
| TC-004 | AC-5-002 | Renderer performs no I/O | Call `render(items, lookback_days=7)` | No file written, no network call, no state mutation; result depends only on inputs | HIGH | Static analysis + pure function (no side effects in implementation) | PASS |
| TC-005 | AC-5-003 | `renderer.py` imports only `osspulse.models` and stdlib | Parse `renderer.py` via `ast` | Zero imports with prefix `osspulse.github`, `osspulse.state`, `osspulse.summarizer`, `osspulse.cache` | HIGH | `ast.walk` collecting all `Import`/`ImportFrom` nodes; assert violations == [] | PASS |
| TC-006 | AC-5-003 | `__init__.py` has no upstream imports | Parse `render/__init__.py` via `ast` | Zero forbidden prefix imports | HIGH | Same `ast`-based static check | PASS |
| TC-007 | AC-5-004 | Re-rendering same input yields byte-equal output (EC-006) | Two calls: `render(items, lookback_days=7)` with same items list | `first == second` (byte-for-byte) | HIGH | Assert `render(items) == render(items)` on multi-repo mixed-type input | PASS |
| TC-008 | AC-5-004 | Double render of empty input is also byte-equal | `render([], lookback_days=7)` twice | Both strings equal | HIGH | Assert `render([]) == render([])` | PASS |
| TC-009 | AC-5-005 | Repos ordered alphabetically regardless of input order (EC-007) | `items = [item(repo="zeta/b"), item(repo="alpha/a")]` | `result.index("## alpha/a") < result.index("## zeta/b")` | HIGH | Position comparison in output string | PASS |
| TC-010 | AC-5-005 | Shuffled input → identical output (EC-007 adversarial) | Same items shuffled with seeds 0, 12345, 99999 (20 repos) | All shuffled renders == canonical render | HIGH | 3 adversarial seed checks independently verified | PASS |
| TC-011 | AC-5-005 | Case-insensitive sort: `Alpha/A` sorts before `zeta/b` (EC-010) | `items = [item(repo="zeta/b"), item(repo="Alpha/A")]` | `result.index("## Alpha/A") < result.index("## zeta/b")` | MEDIUM | Position comparison | PASS |
| TC-012 | AC-5-006 | Item-type groups in fixed Issues→Discussions→Releases order | Repo has items of types `release`, `issue`, `discussion` (input in that order) | issue_pos < disc_pos < rel_pos | HIGH | Position check on `### Issue mới`, `### Discussion`, `### Release` | PASS |
| TC-013 | AC-5-006 | All known + unknown renders Issues→Discussions→Releases→Khác (EC-014) | Items of types `commit`, `release`, `issue`, `discussion` in same repo | issue_pos < disc_pos < rel_pos < other_pos | HIGH | Position check including `### Khác` | PASS |
| TC-014 | AC-5-007 | Items within a group preserve input order (#123 before #120) | `items = [item(id="123"), item(id="120")]` in same repo+type | `- #123` appears before `- #120` | HIGH | Position comparison on item lines | PASS |
| TC-015 | AC-5-007 | Items with descending IDs keep descending order in output (EC-008) | Items with ids [9,1,5,3] | Output item lines in order `#9`, `#1`, `#5`, `#3` | HIGH | Extract `_item_lines()`, assert `[line.split()[1] for line in lines] == ["#9","#1","#5","#3"]` | PASS |
| TC-016 | AC-5-008 | Empty list returns "No new items" doc with title, no `##` (EC-001) | `render([], lookback_days=7)` | Contains `"# OSS Pulse Digest"`, `"No new items in the last 7 days"`, no `"## "` | HIGH | String content assertions | PASS |
| TC-017 | AC-5-008 | "No new items" message reflects lookback_days | `render([], lookback_days=30)` | `"No new items in the last 30 days" in result` | MEDIUM | String content check | PASS |
| TC-018 | AC-5-009 | Empty input never returns `""` or whitespace-only | `render([], lookback_days=7)` | `result != ""` and `result.strip()` is truthy | HIGH | Two assertions | PASS |
| TC-019 | AC-5-010 | Repo absent from items produces no `##` section (EC-015) | Items only for `alpha/a` | `"## alpha/a" in result`, `"## zeta/b" not in result` | MEDIUM | Positive + negative string checks | PASS |
| TC-020 | AC-5-010 | Only repos with items get a `##` section | Single-item list for `present/repo` | Exactly 1 `## ` line in output | MEDIUM | Count `##`-prefixed lines | PASS |
| TC-021 | AC-5-011 | Repo with only issues has no Discussion or Release headers | One `issue` item for `r/x` | `"### Issue mới" in result`, `"### Discussion" not in result`, `"### Release" not in result` | MEDIUM | Positive + negative assertions | PASS |
| TC-022 | AC-5-012 | Canonical line byte-match for full item | `item_id="123"`, `title="Fix bug"`, `summary="Handle null pointer."`, `url="https://gh/123"` | Line `- #123 "Fix bug" — Handle null pointer. [link](https://gh/123)` appears exactly | HIGH | Exact string substring match | PASS |
| TC-023 | AC-5-012 | Summary segment uses U+2014 em-dash, not ASCII hyphen | Any item with non-empty summary | `"—"` (U+2014) in result, not `"-"` as separator | HIGH | Character-level assertion | PASS |
| TC-024 | AC-5-013 | Repo header is exactly `## {repo} — {lookback_days} ngày qua` | `render([item(repo="alpha/a")], lookback_days=7)` | `"## alpha/a — 7 ngày qua" in result` | HIGH | Exact substring match | PASS |
| TC-025 | AC-5-014 | Group header shows correct count: 3 issues → `### Issue mới (3)` | 3 issue items for same repo | `"### Issue mới (3)" in result` | MEDIUM | Exact substring match | PASS |
| TC-026 | AC-5-015 | Empty title omits the quoted-title segment (AC-5-015) | `title=""`, `summary="A summary."`, `url="https://gh/1"` | Line `- #1 — A summary. [link](https://gh/1)`, no `""` quotes | HIGH | Exact substring match + absence of `""` | PASS |
| TC-027 | AC-5-016 | Empty url omits `[link](...)` suffix (AC-5-016) | `title="T"`, `summary="S."`, `url=""` | Line `- #1 "T" — S.`, no `[link]` | HIGH | Exact substring match + absence of `[link]` | PASS |
| TC-028 | AC-5-017 | Empty summary omits `— summary` segment (AC-5-017) | `summary=""`, `title="T"`, `url="https://gh/1"` | Line `- #1 "T" [link](https://gh/1)`, no em-dash in item line | HIGH | Item-line-only assertion (avoid repo header em-dash false positive) | PASS |
| TC-029 | AC-5-017 | Whitespace-only summary treated as empty (AC-5-017) | `summary="   "` | No em-dash in item line | MEDIUM | `_item_lines()` filter + em-dash absence | PASS |
| TC-030 | AC-5-018 | All-empty fields except item_id renders `- #{item_id}` without raising (EC-002) | `title=""`, `url=""`, `summary=""`, `item_id="5"` | `lines[0] == "- #5"` (exactly), no exception | HIGH | Exact string comparison, no exception raised | PASS |
| TC-031 | AC-5-019 | Unknown item_type bucketed under `### Khác (1)` (EC-011) | `item_type="commit"` | `"### Khác (1)" in result`, `"- #7" in result`, no known-type group headers | HIGH | Positive + negative assertions | PASS |
| TC-032 | AC-5-020 | Every input item appears exactly once; duplicate ids both rendered (EC-009) | 5 items; also 2 items with same `item_id="123"` | 5 item lines for N=5; 2 lines and `### Issue mới (2)` for duplicate case | HIGH | `len(_item_lines(result))` == N; group count check | PASS |

---

## EC Spot-checks (QA-added, non-AC)

| TC-ID | EC-ID | Scenario | Result | Notes |
|-------|-------|----------|--------|-------|
| TC-EC-004 | EC-004 | 10k items, 100 repos, 3 types → no crash, 10000 item lines, byte-equal double render | PASS | Output 377,237 chars; both renders identical |
| TC-EC-007A | EC-007 | 20 repos, adversarial shuffle seeds 0/12345/99999 → identical output | PASS | All 3 seeds produce canonical output |
| TC-RF1 | RF-1 | No `set()` call in `renderer.py` source | PASS | `re.findall(r'\bset\s*\(', src)` returns [] |
| TC-RF2A | RF-2 | Empty title + empty url + non-empty summary → `- #1 — S.` | PASS | Combined permutation verified |
| TC-RF2B | RF-2 | Empty title + empty summary + non-empty url → `- #2 [link](https://x)` | PASS | Combined permutation verified |
| TC-RF2C | RF-2 | Empty url + empty summary + non-empty title → `- #3 "T"` | PASS | Combined permutation verified |
