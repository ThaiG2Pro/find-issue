# Handoff: S5 → S6
Generated by: qa | Date: 2026-07-06T16:16:49+07:00

## 1. Key Decisions (with reasoning)

- **GO — 0 bugs, all 22 ACs verified, 459 tests green, coverage 96%**
  WHY: Independent QA run confirmed 459/459 tests pass. All three ADR-001/ADR-003/R1 tripwires confirmed valid. Inner guard deviation (two-arm pattern for CollectorError) confirmed correct and intentional — AuthError/RateLimitError propagate, other CollectorError subclasses absorbed. Renderer GROUP_ORDER unchanged (["issue","discussion","release"]) and GitHubClient Protocol frozen — both confirmed by direct code inspection.
  REJECTED: NO-GO considered for documentation gap (README note about Releases) but rejected because it was never a formal AC or task.

- **RISK-002 tripwire test status: PASSES (correct behavior)**
  WHY: The test asserts that a release with old `created_at` / recent `published_at` is NOT returned. The test PASSING means the accepted miss is in place and the stop key is correctly on `created_at`. This is a documentation test — the test passing = the bug is absent.
  WATCH: If this test ever FAILS in future, the stop key was reversed to `published_at` (Option-B bug).

## 2. Contentious Points

- **Inner guard catch tuple deviation**: Design spec said `except (InvalidRepoError, NetworkError, CollectorError)`. Implementation uses two-arm: `except (InvalidRepoError, NetworkError)` + `except CollectorError` with `isinstance(exc, (AuthError, RateLimitError)) → raise`. This is correct because `AuthError ⊂ CollectorError` — the design spec would silently swallow fatal errors. Confirmed correct by tripwire test. This is a documented, required deviation.
  FINAL: Two-arm pattern is correct and intentional. Do not "simplify" back to a single-tuple.
  WATCH: Any future refactor of the except arms risks re-introducing the fatal-error swallow.

- **README documentation gap**: proposal.md mentions "README gains a short note about Releases". This was not in tasks.md or formal ACs. Readme currently has no mention of releases.
  FINAL: Borderline — it's in the proposal description but not a formal AC. Not a release blocker.
  WATCH: Consider adding in S6 release notes or as a follow-up task.

## 3. Implicit Assumptions

- **RISK-002 tripwire test correctness**: The test correctly documents an ACCEPTED miss. The test is named "...is_missed" and asserts the release is NOT in results. A future developer reading the test name might misinterpret it as a failure case to be fixed — the docstring explicitly clarifies this is intended behavior.
- **pip-audit unavailable**: Dependency vulnerability scanning could not use pip-audit (not installed). Key dependencies (httpx 0.28.1, litellm 1.89.3, redis 8.0.0) are recent maintained versions with no known critical CVEs — assessed by manual inspection only.
- **Smoke test scope**: The integration smoke test confirmed startup, --help, and fail-fast on missing GITHUB_TOKEN. The actual release fetch path was not tested against a live GitHub API (as per policy — tests mock the GitHub client).

## 4. Risky Areas

- **ADR-001 stop key fragility**: The `created_at` vs `published_at` stop/include duality is a subtle gotcha. The RISK-002 tripwire test guards against regression, but the test name could mislead future readers. Comment in `fetch_releases` is the authoritative explanation.
- **Inner guard catch breadth**: `isinstance(exc, (AuthError, RateLimitError))` covers current known fatal subclasses. A future new subclass of `CollectorError` that is also fatal would need updating the isinstance check. Low risk given stable hierarchy — but worth noting for future changes to `errors.py`.
- **README gap**: Minor but visible omission. If a user reads the README, they won't know the digest includes releases until they run it. Low impact for a personal tool.
- **pip-audit gap**: No automated CVE scan was possible. This is an existing project-level tooling gap, not a V2-003 issue.

## 5. Recommended Reading Order (for S6 release prep)

1. `qa-report.md` — full QA session results, AC coverage map, security audit
2. `_decisions.jsonl` — S5 verification entries (QA-VERIFY-001..003, QA-OBS-001)
3. `dev-test-report.md` §Tripwire Tests — the three tripwires and what they guard
4. `src/osspulse/github/client.py` `fetch_releases` docstring — ADR-001 dual-key explanation
5. Skip: `tests/github/` (already QA'd, all green)
