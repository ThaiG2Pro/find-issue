# Handoff — v2-005-push-delivery

Generated by: qa
Title: S5 → S6

## 1. Key Decisions (GO/NO-GO reasoning)

**Decision: GO.**

- 0 Critical / 0 High bugs found across code review, test review, security audit, and smoke tests.
- 15/15 ACs independently verified by QA (code read + smoke + unit tests).
- 103/103 tests pass on independent QA run — matches dev-test-report claim exactly.
- Coverage 96% ≥ 80% threshold.
- Security: SSRF guard (https + host allowlist), URL-leak guard (error messages use status/type, not str(exc)), HTTPS enforcement, ~10s timeout — all verified.
- Two SHALLOW_TC findings (SH-001/002) are low-risk, non-blocking.
- One accepted design behavior (RISK-1: partial multi-message, no rollback) confirmed implemented as designed.

## 2. Contentious Points (borderline / [SPEC-UNCLEAR])

- **Pipeline discord branch untested at pipeline level** (lines 291-294): developer and QA both flagged this. The `elif "discord"` wiring is a single line, and `DiscordDelivery` is well-tested (24 tests) independently. Accepted as non-blocking for this change; recommend a pipeline integration fixture in a follow-up (not a bug, not a gap).
- **RISK-1 partial multi-message**: accepted design per CLAR-3 + ADR. `test_multi_message_second_fails_after_first_sent` verifies this is the actual behavior. Not a bug.
- **`_enforce_limit` safety-net (line 112) never hit**: defensive guard, unreachable in practice. Not a coverage gap — it is intentionally unreachable.

## 3. Implicit Assumptions (test limitations)

- **No live Discord POST tested**: all delivery tests use a mock `httpx.Client`. A real-webhook smoke (with an actual test webhook URL) was not performed — no test webhook was available in the QA environment.
- **pip-audit/safety not available**: dependency audit was manual (httpx 0.28.1, no known CVEs). CI should add `pip-audit` as a gate.
- **No multi-process / concurrent pipeline test**: the pipeline runs single-threaded, so concurrency for delivery is not a concern.

## 4. Risky Areas (fragile-but-passing)

- **`discord_delivery.py:160-161`** (`_char_slice` fallback in `_split_lines`): exercised indirectly by `test_pathological_single_line_is_char_sliced` but the specific lines not directly hit. If the split logic is refactored, this path needs direct coverage.
- **`test_custom_limit_boundary`** [SH-001]: does not assert content preservation at custom limit. If `_split_for_discord` is modified to use a different limit (e.g. configurable per provider), this test would not catch content loss.
- **httpx exception hierarchy**: `TimeoutException` is caught before `RequestError`. If httpx restructures its exception hierarchy in a future version, the ordering could break silently (tests would still pass but wrong message emitted). Pin httpx or add a regression test that specifically checks TimeoutException is NOT matched by the RequestError branch.

## 5. Recommended Reading Order (for S6 release prep)

1. `_release-notes.md` — full S6 task list (CHANGELOG, README, .env.example, pyproject version, git ops)
2. `qa-report.md` — GO decision + security audit (for PR description)
3. `qa/spec_tc_gap_report.md` — AC coverage matrix (for release notes)

## S6 Tasks (developer)

1. Checkout `feature/V2-005-push-delivery`
2. Edit `CHANGELOG.md` — prepend `[0.8.0]` entry (see `_release-notes.md §1`)
3. Edit `README.md` — add `DISCORD_WEBHOOK_URL` row to env table + discord comment to `[output]` block
4. Edit `.env.example` — append Discord delivery section
5. Edit `pyproject.toml` — bump version `0.7.0` → `0.8.0`
6. Run `uv run pytest tests/ -q` + `uv run ruff check .` → confirm clean
7. Commit: `release: v0.8.0 — Discord webhook push delivery (V2-005)`
8. Push + create PR via `gh pr create`
9. Run `openspec archive v2-005-push-delivery`
