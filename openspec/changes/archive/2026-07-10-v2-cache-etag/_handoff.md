# Handoff: S5 → S6
Generated by: qa | Date: 2026-07-10T17:10:00+07:00

> CR V2-007 — GitHub HTTP ETag conditional-request caching. S5 GO: 0 bugs, 28/28 ACs verified,
> 609/609 tests, 97% coverage, RISK-001 tripwire ✅, GraphQL untouched ✅, best-effort semantics ✅.

## 1. Key Decisions (GO reasoning)

- **GO — 0 bugs found**: Full independent code review of all 5 flagged risky areas (RISK-001 commit placement, _classify(304) branch, best-effort load, GraphQL isolation, two-flag gate) plus all 3 new test files. No defect identified.
  WHY: All 28 ACs covered with substantive assertions; RISK-001 tripwire tests verify both the success and crash-safety invariants of commit() placement.
  REJECTED: Deferring smoke test — substituted by 3 E2E pipeline tests (AC-V2-007-026/027/028) using real file stores; equivalent coverage for a CLI tool with no HTTP API surface.

- **DEVIATION-001 accepted**: `_NullConditionalCache` in `ports.py` (not `etag_store.py`). Architecturally correct — preserves AC-2-015 pure-I/O boundary. No functional impact.
  WHY: Existing `test_collector_is_pure_io_no_state_or_llm` enforces the boundary; re-export from `etag_store.py` maintains backward compatibility.

## 2. Contentious Points

- **Smoke test not booted locally**: The integration smoke test (Step 4C: boot app) was not performed because the project's `config.toml` does not currently have an `[etag_cache]` section to exercise and there is no `etags.json` in the repo. The E2E tests in `tests/test_pipeline.py` (AC-V2-007-026/027/028) use real `JsonFileStateStore` + real `JsonFileETagStore` on tmp_path and cover the integration path adequately. This is not a blocker — it is a structural limitation of a CLI tool with no always-on server.
  FINAL: Acceptable for lite rigor. Noted as a verification gap in the report.
  WATCH: S6 release prep should confirm config.toml README example includes `[etag_cache]`.

- **No new runtime dependency**: `etag_store.py` is stdlib-only. No version pinning issue.

- **ADR-003 `_classify(304)→OK` subtlety**: The dual-mapping (200 and 304 both → OK) is a subtle trap for future maintainers. The code comment in `client.py` and the `_handoff.md` flag are the guard. The test coverage is implicit (the test passes only because the code branches on status_code, not action).
  FINAL: Accepted — comment + test constitute sufficient documentation.
  WATCH: Any future refactor of `_classify` that adds a `NOT_MODIFIED` action enum value must also update the fetch method branch condition.

## 3. Implicit Assumptions

- `pip-audit` / `safety` not available in this environment — no new packages added so vulnerability audit is unblocked (LOW risk).
- The `test_commit_mkdir_failure_warns_and_does_not_raise` test patches `os.replace` (not `mkdir`) to simulate the mkdir failure path. This works because `mkdir` succeeds (tmp_path exists) but `os.replace` fails — the WARN is still logged. Slightly indirect but the intent (best-effort on any write OSError) is covered.
- `test_store_does_not_import_json_store` checks import lines at the module text level. A dynamic import (`importlib.import_module("osspulse.state.json_store")`) would not be caught. This is acceptable — no reason for dynamic import here, and the test covers the realistic case.

## 4. Risky Areas (passed but fragile)

- **commit() placement**: Still the single most critical line in the change. The code comment + RISK-001 tripwire tests are the only guards. Future contributors adding a try/except around `_collect_all` that also swallows the commit would break the crash-safety guarantee without a test failure (the commit would still run). The `test_commit_not_called_on_auth_error_mid_loop` test guards the propagation path, not a try/except wrap. Consider adding a code comment at `_collect_all`'s call site warning against try/except wrapping.

- **`_classify(304)→OK` + fetch branch coupling**: If someone adds a `NOT_MODIFIED` action to `_Action` and changes `_classify` to return it for 304, but forgets to update the fetch method branch from `response.status_code == 304` to the new action, the 304 handling silently breaks (no item returned, but no error). The existing test catches this IF the mock returns `content=b""` (which it does). Fragile only if the mock response is changed.

- **etags.json file location**: The default path `./.osspulse/etags.json` is relative to the working directory. If `osspulse run` is called from a different directory than the config file's directory, etags.json and state.json will live in different locations. This is pre-existing behavior (mirrors state_path default) and is not a regression from this CR.

## 5. Recommended Reading Order for S6 (release prep)

1. `qa-report.md` — S5 GO decision + full AC coverage matrix
2. `src/osspulse/pipeline.py` §run_pipeline — commit() placement comment (the release notes warning)
3. `openspec/changes/v2-cache-etag/proposal.md` §"What Changes" — README update note (etag_cache best-effort opt-in section needs to be confirmed in README)
4. `dev-test-report.md` — 60 new tests, deviation summary for the changelog
5. Skip: `design.md §Sketch` — implementation complete, diagram no longer needed for release
