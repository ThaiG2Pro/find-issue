# Handoff: S5 → S6
Generated by: qa | Date: 2026-07-13

## 1. Key Decisions (GO/NO-GO reasoning)

**GO — 0 bugs, 15/15 ACs verified, 727 tests pass, coverage 90%.**

All four bundled tweaks (retry x7, pre-summarize truncation, truncation alert, Option-A per-item embeds) are implemented correctly and tested thoroughly. The byte-identical no-op invariant holds for render() and the pipeline. T1 (URL never in DeliveryError) is preserved in the reshaped embed POST path. mark_seen full-set idempotency is a structural invariant enforced by placement in pipeline.py (not a comment).

## 2. Contentious Points (borderline bugs / SPEC-UNCLEAR)

None. All five S4 deviations are minor additive improvements with no AC conflict:
- `dropped_counts` nested shape `dict[str, dict[str, int]]` — renderer sums correctly; no AC specifies internal shape.
- Alert placement logic using `sum(repo_drops.values()) > 0` — correct for multi-type drops.
- Zero-items fallback trigger on parsed-item count, not section count — more conservative, correct.
- `re` local import inside `_parse_sections` — harmless, Python-cache-safe.
- `test_embed_batching_sends_multiple_requests` replaced by `test_embed_batching_10_items_plus_header` — old test was incorrect (zero parsed items → plain-text fallback, not embeds); new test covers actual AC-V4-002-008 scenario.

## 3. Implicit Assumptions (test limitations)

- Full live smoke test (real GitHub API + Discord webhook + LLM) was not run — `rigor=lite` / `scope=tiny` and no live credentials in CI context. All boundary conditions covered by unit tests.
- `test_surviving_items_preserve_input_order` uses a 5-item / cap=4 set. Sufficient for the AC, but a larger shuffle set would further stress the filter-in-place contract. Non-blocking.
- `test_no_upstream_imports` inspects `sys.modules` at test time — reliable for module-level imports; would miss a lazy `import` deep inside a runtime code path (none found in review).

## 4. Risky Areas (passed but fragile)

- **Parser–renderer coupling**: `_ITEM_RE` regex in `_parse_sections` is coupled to `_build_item_line` format (`- #{id} "{title}" — {summary} [link]({url})`). If the renderer's item line format changes, the parser must be updated in the same PR. The zero-items fallback is the safety net, not a license to skip the paired update.
- **`dropped_counts` key matching**: must remain `item.raw.repo` string throughout any future refactor. Mismatch silently suppresses the truncation alert (no error raised).
- **Embed count growth**: current max is 10 items/type × N types × M repos + M header embeds. With default cap=10 and many repos, batching load grows linearly. No issue at current scale; worth noting for V3+ multi-repo scenarios.

## 5. Recommended Reading Order (for S6 release prep)

1. `qa-report.md` — full AC verification table + smoke test notes
2. `dev-test-report.md` — coverage breakdown per module
3. `src/osspulse/pipeline.py` — truncation placement (lines 436–447) if release includes a final pipeline audit
4. `src/osspulse/delivery/discord_delivery.py` — parser–renderer coupling gotcha (§4 above)
