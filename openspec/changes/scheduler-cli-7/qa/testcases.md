# Test Cases — scheduler-cli-7 (osspulse run V1 pipeline)

> Generated by: qa · Phase: S5 · 2026-06-30
> Spec: `specs/scheduler-cli/spec.md` — 22 ACs · Design: `design.md` ADR-001..006

## Coverage Summary

| Status | Count |
|--------|-------|
| COVERED (Dev) | 17 |
| SHALLOW_TC — QA verify | 4 (AC-7-013, AC-7-018, AC-7-021, AC-7-009) |
| BOTH_MISS | 0 |
| Total ACs | 22 |

---

## TC-001 — Happy path: one combined render + deliver (AC-7-001, AC-7-016)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-001, AC-7-016 |
| Priority | High |
| Dev coverage | `test_happy_path_llm_one_combined_call` |
| QA method | Code review |

**How to verify**: Read `run_pipeline` — single `render(summarized, lookback_days=...)` call and single `delivery.deliver(digest)` call; no per-repo render/deliver.
**Expected**: One `render()` call receiving the flat combined `list[SummarizedItem]`; one `deliver()` call.
**Result**: ✅ PASS — code review confirmed (pipeline.py lines 205–216).

---

## TC-002 — Import isolation: no stage imports another (AC-7-002)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-002 |
| Priority | High |
| Dev coverage | `test_import_isolation_no_stage_imports_another` |
| QA method | Code review + static test |

**How to verify**: Check that `github/`, `state/`, `summarizer/`, `cache/`, `render/`, `delivery/` contain no cross-imports.
**Expected**: Only `pipeline.py` imports across stages.
**Result**: ✅ PASS — code review confirms `pipeline.py` is the only cross-stage importer; static test passes.

---

## TC-003 — NotImplementedError stub replaced (AC-7-003)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-003 |
| Priority | High |
| Dev coverage | `test_happy_path_llm_one_combined_call` |
| QA method | Code review |

**How to verify**: `pipeline.py` contains no `raise NotImplementedError`; `cli.py` does not `deliver("osspulse: pipeline not yet implemented")`.
**Expected**: Real orchestration runs; stub string absent.
**Result**: ✅ PASS — confirmed in source.

---

## TC-004 — Per-repo isolation: InvalidRepoError / NetworkError skip (AC-7-004)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-004, BR-7-001 |
| Priority | High |
| Dev coverage | `test_invalid_repo_error_skipped_others_succeed`, `test_network_error_skipped`, `test_generic_collector_error_skipped` |
| QA method | Code review |

**How to verify**: In `_collect_all`, `except (InvalidRepoError, NetworkError, CollectorError)` branch increments `skipped`, logs WARN with `type(exc).__name__`, and continues loop.
**Expected**: No re-raise; other repos still processed.
**Result**: ✅ PASS — confirmed (pipeline.py lines 150–154).

---

## TC-005 — AuthError is fatal (AC-7-005)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-005, BR-7-002 |
| Priority | Critical |
| Dev coverage | `test_auth_error_is_fatal`, `test_auth_error_exits_1_no_token_in_message` |
| QA method | Code review |

**How to verify**: `except AuthError` is the FIRST clause in the ladder (before `RateLimitError` and `CollectorError`); re-raises; `cli.py` catches it and exits 1.
**Expected**: `AuthError` re-raised immediately; second repo never attempted; CLI exits 1.
**Result**: ✅ PASS — ADR-003 order verified (pipeline.py lines 135–141); CLI except confirmed (cli.py lines 26–30).

---

## TC-006 — All repos fail → no-new-items doc delivered, exit 0 (AC-7-006)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-006 |
| Priority | High |
| Dev coverage | `test_all_repos_fail_delivers_no_new_items` |
| QA method | Code review |

**How to verify**: `render([], lookback_days=7)` returns non-empty "No new items in the last 7 days" doc; `delivery.deliver()` called with it.
**Expected**: Non-empty digest delivered; no raise; exit 0.
**Result**: ✅ PASS — confirmed by renderer source (`render/renderer.py:74-80`) + test.

---

## TC-007 — Batch summarize: exactly one summarize_items call (AC-7-007)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-007, BR-7-009 |
| Priority | High |
| Dev coverage | `test_exactly_one_summarize_one_render_one_deliver` |
| QA method | Code review |

**How to verify**: `_summarize()` calls `summarizer.summarize_items(all_items)` once; never calls per-item `summarize()`.
**Expected**: One `summarize_items` call with the full combined list.
**Result**: ✅ PASS — confirmed (pipeline.py lines 183–186).

---

## TC-008 — No-LLM path: placeholder visible in digest (AC-7-008, AC-7-022)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-008, AC-7-022, BR-7-010 |
| Priority | High |
| Dev coverage | `test_no_llm_path_summarizer_never_constructed`, `test_no_llm_placeholder_constant` |
| QA method | Code review |

**How to verify**: When `llm_provider is None`, `_summarize()` returns `[SummarizedItem(raw=item, summary=NO_LLM_PLACEHOLDER) for item in all_items]` without touching `LiteLLMSummarizer`.
**Expected**: `LiteLLMSummarizer` never constructed; `"(no summary — LLM disabled)"` present in rendered output.
**Result**: ✅ PASS — confirmed (pipeline.py lines 172–174); placeholder constant value verified.

---

## TC-009 — Redis unreachable: _NullCache degrades gracefully (AC-7-009) [SHALLOW_TC → QA verify]

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-009 |
| Priority | Medium |
| Dev coverage | `test_redis_unreachable_degrades_gracefully` (only `_NullCache` API); `test_build_cache_returns_null_cache_on_redis_error` |
| QA method | Code review + integration flow |

**Gap**: Dev tests verify `_NullCache.get → None` and `_build_cache` returns something with `.get → None` on bad URL. But no test runs the full `run_pipeline` with a real Redis failure and verifies the end-to-end digest is delivered.

**How to verify**: Code review — when `_build_cache()` catches an exception, it returns `_NullCache()`; `_summarize()` passes this as `cache=` to `LiteLLMSummarizer`; summarizer's cache-aside degrades (its `_cache_get` already swallows Redis errors — summarizer-llm-4 ADR-004).

**Expected**: Run produces digest; no crash; one WARN log about Redis unavailability.
**Result**: ✅ PASS via code review — `_build_cache` exception path returns `_NullCache()` (pipeline.py:94-97); downstream summarizer independently degrades.

---

## TC-010 — mark_seen per repo, decoupled from summarize (AC-7-010, AC-7-019)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-010, AC-7-019 |
| Priority | High |
| Dev coverage | `test_happy_path_mark_seen_called_per_repo`, `test_mark_seen_decoupled_from_summarize` |
| QA method | Code review |

**How to verify**: In `_collect_all` loop, `state.mark_seen(items)` is called BEFORE `all_items.extend(items)` and BEFORE any summarization. A later summarize failure does not roll back seen state.
**Expected**: `mark_seen` called per-repo at collect time; decoupled from summarize outcome.
**Result**: ✅ PASS — confirmed (pipeline.py lines 128–131).

---

## TC-011 — V1 does not filter seen items (AC-7-011)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-011, BR-7-011 |
| Priority | High |
| Dev coverage | `test_idempotent_rerun_same_digest` |
| QA method | Code review |

**How to verify**: Pipeline does NOT call `is_seen()` to filter items; only `mark_seen()` for recording. No filtering logic in `_collect_all`.
**Expected**: Re-run produces byte-identical digest; same items rendered again.
**Result**: ✅ PASS — `is_seen()` never called in pipeline.py; confirmed.

---

## TC-012 — ConfigError exits 1 with Error: message (AC-7-012)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-012, BR-7-004 |
| Priority | High |
| Dev coverage | `test_config_error_exits_1_no_traceback` |
| QA method | Code review |

**How to verify**: `cli.py except ConfigError` → `typer.echo(f"Error: {e}", err=True)` + `Exit(code=1)`.
**Expected**: `Error:` on stderr, no traceback, exit 1.
**Result**: ✅ PASS — confirmed (cli.py lines 38–40).

---

## TC-013 — BrokenPipeError exits 0 cleanly (AC-7-013) [SHALLOW_TC — known deviation]

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-013 |
| Priority | Medium |
| Dev coverage | `test_broken_pipe_exits_0` (static inspection only) |
| QA method | Code review + manual smoke |

**Gap**: Dev test is static-only (CliRunner limitation). QA verifies by code review.

**How to verify (code review)**: `cli.py except BrokenPipeError` → `os.dup2(os.open(os.devnull, os.O_WRONLY), sys.stdout.fileno())` + `raise typer.Exit(code=0)`. Handler appears FIRST in the except ladder.

**Expected**: No traceback; exit 0; devnull redirect prevents flush error.
**Result**: ✅ PASS via code review — handler present and correct (cli.py lines 18–23). Manual smoke outside scope of this environment (no real pipe available).

---

## TC-014 — No secret in logs/stderr/digest (AC-7-014) [QA extend to AuthError path]

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-014, BR-7-012 |
| Priority | Critical |
| Dev coverage | `test_no_secret_in_logs_stderr_or_digest` (NetworkError path) + `test_auth_error_exits_1_no_token_in_message` |
| QA method | Code review — AuthError re-raise path |

**Gap**: Dev log-capture test covers the NetworkError skip path. QA verifies the AuthError re-raise path by code review.

**How to verify**: When `AuthError` is re-raised, `cli.py` catches it and calls `typer.echo(f"Error: {e}", err=True)`. The `AuthError` message is `"GitHub auth failed for '{repo}' (status {code})"` — repo name + status code only, constructed in `github/client.py:154`. Token never interpolated.

**Expected**: Token not in stderr on AuthError.
**Result**: ✅ PASS — `AuthError` message confirmed token-safe (client.py:154-156).

---

## TC-015 — Per-repo outcome log, one line per repo (AC-7-015)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-015, BR-7-005 |
| Priority | High |
| Dev coverage | `test_per_repo_outcome_log_emitted` |
| QA method | Code review |

**How to verify**: Each `try` branch in `_collect_all` emits exactly one `logger.info` or `logger.warning` per repo; no branch emits more than one.
**Expected**: Exactly one log line per repo.
**Result**: ✅ PASS — each branch (success, skip, rate-limit) emits exactly one log call (pipeline.py lines 133, 138, 144, 153).

---

## TC-016 — RateLimitError: stop collection, deliver partial (AC-7-017)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-017, BR-7-008 |
| Priority | High |
| Dev coverage | `test_rate_limit_terminal_delivers_partial` |
| QA method | Code review |

**How to verify**: `except RateLimitError` increments `skipped`, logs WARN, and `break`s the loop — does NOT `raise` or `return`. Execution continues to `_summarize` → `render` → `deliver`.
**Expected**: Items already collected are rendered and delivered; exit 0.
**Result**: ✅ PASS — `break` confirmed (pipeline.py:148); no re-raise.

---

## TC-017 — Summarizer returns fewer items: only survivors rendered (AC-7-018) [SHALLOW_TC → QA verify]

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-018 |
| Priority | Medium |
| Dev coverage | `test_summarizer_returns_fewer_items` — asserts `deliver` called but NO content check |
| QA method | Code review |

**Gap**: Dev test does not verify that the delivered digest contains only the survivor item, not all collected items.

**How to verify (code review)**: `_summarize()` returns `summarizer.summarize_items(all_items)` whose return value (survivors only) is passed directly to `render(summarized, ...)`. The pipeline does not merge `all_items` back in.

**Expected**: `render` receives only survivors; digest reflects survivors only.
**Result**: ✅ PASS via code review — `summarized = _summarize(config, all_items)` → `render(summarized, ...)` (pipeline.py:198,205); no re-merge.

---

## TC-018 — DeliveryError exits 1 (AC-7-020)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-020, BR-7-004 |
| Priority | High |
| Dev coverage | `test_delivery_error_exits_1` |
| QA method | Code review |

**How to verify**: `cli.py except DeliveryError` → `typer.echo(f"Error: {e}", err=True)` + `Exit(code=1)`.
**Result**: ✅ PASS — confirmed (cli.py lines 34–36).

---

## TC-019 — Run-summary log line emitted (AC-7-021) [SHALLOW_TC → QA verify]

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-021 |
| Priority | Medium |
| Dev coverage | `test_run_summary_log_emitted_on_success` — only checks `exit_code == 0`; caplog not asserted |
| QA method | Code review |

**Gap**: Dev test does NOT assert the log line content or even that it was emitted — only exit code.

**How to verify (code review)**: `run_pipeline` ends with `logger.info("run complete — repos: %d, collected: %d, summarized: %d, skipped: %d", ...)` after `delivery.deliver(digest)`.

**Expected**: One INFO log line with repos/collected/summarized/skipped counts.
**Result**: ✅ PASS via code review — log call confirmed (pipeline.py lines 222–228); content matches AC-7-021 spec.

---

## TC-020 — StateError exits 1 (implicit in AC-7-012 contract)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-012 |
| Priority | Medium |
| Dev coverage | `test_state_error_exits_1` |
| QA method | Code review |

**Result**: ✅ PASS — `except StateError` → `Exit(code=1)` confirmed (cli.py lines 30–33).

---

## TC-021 — LLM model derived correctly, no Config change (ADR-002)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-007 |
| Priority | Medium |
| Dev coverage | None directly |
| QA method | Code review |

**How to verify**: `_model_for("openai")` returns `"openai/gpt-4o-mini"`; `_model_for("unknown")` returns `"unknown/unknown"`.
**Expected**: `SummarizerConfig(model=_model_for(provider))` never raises; no Config field added.
**Result**: ✅ PASS — `_PROVIDER_MODEL` dict + fallback `f"{provider}/{provider}"` confirmed (pipeline.py lines 40–48).

---

## TC-022 — Security: token/key never on pipeline object (RF-1, BR-7-006)

| Field | Value |
|-------|-------|
| AC-IDs | AC-7-014 |
| Priority | Critical |
| Dev coverage | `test_no_secret_in_logs_stderr_or_digest` |
| QA method | Code review |

**How to verify**: `run_pipeline` passes `config.github_token` only to `GitHubCollector(config.github_token)` and `config.llm_api_key` only to `LiteLLMSummarizer(api_key=config.llm_api_key, ...)`. Neither is stored on a variable that outlives the constructor call.
**Expected**: Token/key not accessible after construction; never logged.
**Result**: ✅ PASS — `collector = GitHubCollector(config.github_token)` (local var, not `self.token`); `LiteLLMSummarizer(api_key=config.llm_api_key, ...)` (local var) (pipeline.py lines 194-196).
